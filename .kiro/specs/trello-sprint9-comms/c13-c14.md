# C13 (#244) polish + C14 (#245) integration tests — working notes

## Shared brand/UX rules for ALL comms views (C13)
- Gold/navy tokens + `ds-*` design-system classes. NO bespoke blue/indigo inline gradients.
  - The pre-existing `parents_contact` blue inline styles are OFF-BRAND → must be fixed, not copied.
- Icons: `<x-svg-icon name="..." :size="N" />` (Bootstrap Icons sprite). No `bi bi-` font spans, no line-awesome.
- Page header: `<h2>` title + breadcrumb (الرئيسية → section → page).
- Cards: `.ds-card` / `.ds-card-header` / `.ds-card-title`.
- Empty states: `.ds-empty` / `.ds-empty-icon` / `.ds-empty-title` / `.ds-empty-desc`.
- Tables: `table table-hover`, action buttons `.ds-action-btn`, count badges `.ds-badge-*`.
- Confirm/Toast are GLOBAL (app.blade.php): plain `confirm('...')` on delete/send forms auto-upgrades to SweetAlert; single-line flash `.alert-success/.alert-danger` auto-toasts. Do NOT hand-roll SweetAlert. Use `window.vcToast`/`window.vcConfirm` only for new dynamic code.
- RTL correct; responsive (mobile/tablet).
- DO NOT touch: controllers, routes, web.php, permission seeder, sidebar.blade.php, navbar.blade.php, form field `name`s, `route()` targets, AJAX endpoints/JS behavior.

## Priority pages (card gives detailed acceptance)
1. whatsapp/send.blade.php — mostly already done; fix indigo chips → navy/gold.
2. sms-services/templates/index.blade.php — empty state, clear add btn, working search.
3. sms-services/templates/form.blade.php — variable CHIPS, searchable, GROUPED (user/student/parent/school/attendance), bigger editor, live counter (has), PREVIEW w/ sample substitution.
4. communications/parents_contact/{index,show} — counts as ds-badges, control as dropdown, send-from-row; remove blue inline styles.

## C14 test matrix baseline (pre-polish, untouched code)
- Permission grid PASS: admin 200; student/parent 403 on mgmt pages; public comms (announcements/discussion/calendar/mailbox) 200 all; teacher 200 on discussion/calendar/virtual-classes.
- Null-school super-admin (school_id=NULL, no scope): ALL comms pages 200, no 500. PASS (regression class clean).
- Pre-existing bug (OUT OF COMMS SCOPE, Sprint-10 attendance): /admin/attendance/calendar → 500 for admin. Flag only.

## Local test mutations (revert/report)
- Set password Mostafa@123 on users 2 (school-admin), 47 (teacher), 49 (student), 50 (parent). LOCAL ONLY.
- Null-school sweep nulled users.id=1 school_id then reverted to 1.
