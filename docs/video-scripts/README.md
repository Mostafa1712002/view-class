# سكربتات فيديوهات الشرح — ViewClass

مكتبة تصوير كاملة: 46 فيديو عبر 6 مسارات حسب الدور. كل سكربت جاهز للتسجيل
(تعليق صوتي بالتوقيت + ترتيب الضغطات + نص الهنت العربي على الشاشة).

- **الخطة الأم:** `../VIDEO_TUTORIAL_PLAN.md`
- **قالب التنسيق:** `track-0-shared.md`
- **اللقطات المرجعية:** `screens/`

## المسارات

| المسار | الملف | فيديوهات | اللقطات |
|--------|------|:---:|:---:|
| 0 — مشترك | `track-0-shared.md` | 4 | ✅ 4 |
| 1 — مدير النظام | `track-1-super-admin.md` | 6 | ✅ 5 |
| 2 — مدير المدرسة | `track-2-school-admin.md` | 15 | ✅ 10 |
| 3 — المعلم | `track-3-teacher.md` | 11 | ✅ 13 |
| 4 — الطالب | `track-4-student.md` | 7 | ✅ 6 |
| 5 — ولي الأمر | `track-5-parent.md` | 3 | ✅ 3 |
| **الإجمالي** | | **46** | **41 لقطة** |

اللقطات تغطي شاشة الهبوط لكل فيديو تقريبًا. القليل المتبقّي شاشات فرعية أو
ديناميكية (تحتاج معرّف) — تُلتقط عند التسجيل.

## 🎬 المكان داخل النظام (Docs)

الفيديوهات لها الآن **مركز داخل التطبيق**: `/docs` (رابط "دليل الاستخدام" في
القائمة الجانبية، لكل الأدوار). يعرض الفيديوهات مفلترة حسب دور المستخدم.
- المصدر: `config/tutorials.php` (كتالوج ثابت — 46 فيديو).
- كل بطاقة تعرض "قريبًا" حتى يُسجَّل الفيديو؛ ضع رابط الفيديو في `video_url`
  بالكونفيج ليُعرَض في مكانه.
- منشور ومتحقَّق live (commit `46eb524`).

## بيئة التصوير

- محلي: `http://viewclass.test` — حسابات تجريبية (كلمة مرور مؤقتة محلية `record123`):
  مدير نظام `admin@goldenplatform.com` · مدير مدرسة `manager@alawwal.local` ·
  معلم `seed_teacher_1@local.test` · طالب `test_student_001@viewclass.local` ·
  ولي أمر `test_parent_001@viewclass.local`.

## ⚠️ فروقات المواصفات مقابل الكود (اكتشفها الوكلاء أثناء الكتابة)

بنود تحتاج مراجعة الفريق قبل التسجيل — السكربت اتكتب على الواقع مش على المواصفة:

1. **نصاب 35 حصة (V2.6):** يُعرَض في تقرير الأنصبة فقط — **مش مرفوض** بشكل صارم في `SchoolScheduleController`. المواصفة بتقول "النظام يرفض".
2. **علامة الشهادة المائية (V2.14):** ترندر كشعار في الترويسة (`div.logo`, max-height 70px) — **مش خلفية شفافة** عبر الصفحة كما المواصفة.
3. **وزن اختبار ناتج التعليم 30% (V2.12):** قابل للضبط لكل نموذج (`job_perf_settings.weight`) — **مش ثابت** في النظام.
4. **بنك أسئلة المعلم (V3.5):** المعلم **قراءة فقط** (تصفح/معاينة) — لكن زر "إضافة" يرندر دايمًا في الـ Blade (باج UI محتمل). الإنشاء/الاعتماد/الاستيراد لمدير المدرسة.
5. **حجوزات المعلم الواردة (V3.11):** `manage.appointments.index` بلا رابط في قائمة المعلم رغم إتاحة الراوت — وصول بالـ URL المباشر.
6. **رابط القبول والتسجيل (V2.15):** عنصر القائمة `href="#"` رغم إن راوتات `admissions.*` حية — تصوير عبر `/admin/admissions`.

## المرحلة التالية

- التقاط لقطات المسارين 1 و2 (إدارية).
- (اختياري) رفع الفروقات أعلاه ككروت ترلو لمراجعة الفريق.
